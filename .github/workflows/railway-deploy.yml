name: Deploy to Railway and Hostinger

on:
  push:
    branches: [ main, hotel-static-v2 ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  VITE_API_BASE_URL: https://jungle-tours-backend-production.up.railway.app

jobs:
  # 🚀 Deploy Backend to Railway
  deploy-backend:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v4

      - name: 🚂 Install Railway CLI
        run: npm install -g @railway/cli

      - name: 🚀 Deploy to Railway
        run: railway up --service ${{ secrets.RAILWAY_SERVICE_NAME }}
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

  # 🌐 Deploy Frontend to Hostinger via FTP
  deploy-frontend:
    runs-on: ubuntu-latest
    needs: deploy-backend

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🏗️ Build frontend
        run: npm run build:client:prod
        env:
          NODE_ENV: production

      - name: 📤 Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.HOSTINGER_FTP_HOST }}
          username: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          local-dir: ./dist/public/
          server-dir: ./
          dangerous-clean-slate: true
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.DS_Store
            **/README.md
            **/.gitignore

      - name: ✅ Deployment Complete
        run: echo "Frontend deployed to Hostinger successfully"

  # 💾 Backup artifact if FTP fails
  create-artifact:
    runs-on: ubuntu-latest
    needs: deploy-frontend
    if: failure()

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🏗️ Build frontend
        run: npm run build:client:prod
        env:
          NODE_ENV: production

      - name: 💾 Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build-${{ github.run_number }}
          path: dist/public/
          retention-days: 7

  # 🧪 Run basic health checks
  health-check:
    runs-on: ubuntu-latest
    needs: [deploy-backend, deploy-frontend]
    if: success()

    steps:
      - name: 🔍 Check backend health
        run: |
          sleep 30  # Wait for deployment to stabilize
          curl -f https://jungle-tours-backend-production.up.railway.app/health || exit 1

      - name: 🔍 Check frontend accessibility
        run: |
          curl -f -I https://ayahuascapuertonarino.com/ | head -n 1 | grep -q "200\|301\|302" || exit 1

      - name: ✅ Health checks passed
        run: echo "All health checks passed!"
